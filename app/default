user www-data;
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    upstream nzbget_servers {
        server 192.168.1.1:6789;        # Replace with your first NZBGet server IP and port
        server 192.168.1.2:6789;        # Replace with your second NZBGet server IP and port
    }

    server {
        listen 80;                      # Change to 443 for HTTPS
        server_name your.domain.com;    # Replace with your domain

        location / {
            proxy_pass http://nzbget_servers;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
